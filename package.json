{
  "name": "copy-paste-value-extension",
  "version": "0.0.3",
  "description": "",
  "license": "MIT",
  "author": "",
  "scripts": {
    "build": "yarn prettier && zip -r app.zip src/*",
    "prettier": "prettier --write src/**/*.{js,json,html} && prettier-package-json --write package.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.0.2",
    "prettier-package-json": "^2.8.0"
  }
}
